{% extends 'base.html.twig' %}
{% block title %} Page des joueurs{% endblock %}
    {# commentaire twig #}
{% block body %}
 <h1><p>List des joueurs:</p></h2>
    <table class="table table-striped">
      {% for player in players %}
      <tr>
        <td>
           <a href="/player/{{player.id}}" > {{player.name}}</a>
      </td>
        <td>{{player.num}}</td>
        <td>
        {% if player.substitute %}
          <span>Remplaçant</span> {% else %}
          <span>Titulaire</span> {% endif %}
        </td>
      </tr>
      {% endfor %}
    </table>

  {% endblock %}
